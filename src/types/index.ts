export interface course {
  name: string;
  code: string;
  description: string;
  credits: number;
}

export interface class_ {
  startTime: string;
  endTime: string;
  dayOfWeek: string;
  location: number;
}
