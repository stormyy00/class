import Calendar from "../../components/weekday/Calendar";
import React from "react";

const page = () => {
  return (
    <div className="flex w-full flex-col items-center justify-center">
      <Calendar />
    </div>
  );
};

export default page;
